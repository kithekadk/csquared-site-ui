import { SearchnewsPipe } from './searchnews.pipe';

describe('SearchnewsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchnewsPipe();
    expect(pipe).toBeTruthy();
  });
});
