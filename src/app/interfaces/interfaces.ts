export interface services{
    image:string;
    title: string;
    description: string;
}

export interface authorities{
    name:string;
    image:string;
    rank: string;
    description: string;
}